package name.dezalator.model.module.weapon;

import name.dezalator.model.module.Weapon;
import name.dezalator.model.module.base.Kinetic;

public class MassDriver extends Weapon implements Kinetic {
    public MassDriver(int damage) {
        super(damage);
    }
}
